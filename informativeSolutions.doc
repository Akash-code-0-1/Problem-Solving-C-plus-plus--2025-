########   Long Number + Math   #######

#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    if(!(cin >> t)) return 0;
    while (t--) {
        long long n;
        cin >> n;
        vector<long long> ans;
        __int128 pow10 = 10; // 10^1
        while (true) {
            __int128 denom = pow10 + 1; // 10^k + 1
            if (denom > (__int128)n) break;
            long long d = (long long)denom;
            if (n % d == 0) {
                long long x = n / d;
                if (x > 0) ans.push_back(x);
            }
            pow10 *= 10;
        }
        if (ans.empty()) {
            cout << 0 << '\n';
        } else {
            sort(ans.begin(), ans.end());
            cout << ans.size() << '\n';
            for (size_t i = 0; i < ans.size(); ++i) {
                if (i) cout << ' ';
                cout << ans[i];
            }
            cout << '\n';
        }
    }
    return 0;
}



